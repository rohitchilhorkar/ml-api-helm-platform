# Staging overrides only what differs from values.yaml.
# Real registry pull (image.pullPolicy stays at the platform default),
# more than one replica to catch rolling-update/multi-pod issues before prod.

image:
  repository: 123456789012.dkr.ecr.us-east-1.amazonaws.com/ml-api

replicaCount: 2
